- 2026-09-26 PI[claude]: my pueue watcher (schedule_task shell, interval 10m, `cd … && pueue status --json | python3 -c … && tail …`) errored in ~0.3s on all 60 runs, including 8h after the jobs were Done; the same command exits 0 by hand. History keeps no stdout/stderr for failed runs, so I could not see why. Also my own bug earlier: `pueue add -- bash -c "…"` loses the quoting; pass one string, pueue already uses a shell.
